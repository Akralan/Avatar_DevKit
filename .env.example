# URL du backend Scaleway. En développement, Vite proxifie /body, /graft et
# /healthz vers cette adresse : le navigateur ne voit qu'une seule origine,
# donc aucune question de CORS.
VITE_API_BASE=https://REMPLACER.functions.fnc.fr-par.scw.cloud
